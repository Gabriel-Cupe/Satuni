{
  "name": "reservation-automation",
  "version": "1.0.0",
  "description": "Sistema de automatización de reservas",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development nodemon server.js",
    "vercel-build": "npm install && echo 'Build completo'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.53.0",
    "compression": "^1.8.0",
    "express": "^4.18.2",
    "path-to-regexp": "^6.2.2",
    "puppeteer-core": "^24.17.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "puppeteer": "^24.17.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
